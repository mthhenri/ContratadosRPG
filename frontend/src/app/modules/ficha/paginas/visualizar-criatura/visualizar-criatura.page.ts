import { Component } from '@angular/core';

/**
 * Placeholder de sequenciamento (m4-04b, Task 5→11): mantém `criatura.routes.ts` resolvendo a um
 * módulo real enquanto as Tasks 6-10 (editores de lista + `CriaturaVisualizacao`) são construídas.
 * Substituído pela implementação real na Task 11 — não é trabalho descartável, só ordem.
 */
@Component({
  selector: 'app-criatura-visualizar',
  template: '',
})
export class CriaturaVisualizar {}
