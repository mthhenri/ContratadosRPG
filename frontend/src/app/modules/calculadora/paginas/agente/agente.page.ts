import { Component } from '@angular/core';

/**
 * Stub da aba "Agente / Civil" da calculadora (m1-06 — fundação). O cálculo de stats do
 * agente entra em task posterior do M1 (ver docs/CONTEXT.md), consumindo `shared/regras/agente`.
 */
@Component({
  selector: 'app-agente-page',
  imports: [],
  templateUrl: './agente.page.html',
  styleUrl: './agente.page.scss',
})
export class AgentePage {}
