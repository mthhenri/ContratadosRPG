import { Component } from '@angular/core';

/**
 * Stub da aba "DT" (dificuldade de teste) da calculadora (m1-06 — fundação). O cálculo entra
 * em task posterior do M1 (ver docs/CONTEXT.md), consumindo `shared/regras/dt`.
 */
@Component({
  selector: 'app-dt-page',
  imports: [],
  templateUrl: './dt.page.html',
  styleUrl: './dt.page.scss',
})
export class DtPage {}
