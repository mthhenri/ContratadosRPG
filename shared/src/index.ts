/**
 * Barrel de entrada do pacote `@contratados-rpg/shared`.
 *
 * Por enquanto expõe apenas uma constante trivial que valida a ligação de
 * workspace entre `backend/` e `frontend/`. Os barrels reais (dtos, enums,
 * interfaces, validators, regras) nascem nas tasks/milestones que os consomem.
 */
export const SHARED_PACKAGE_NAME = '@contratados-rpg/shared';
