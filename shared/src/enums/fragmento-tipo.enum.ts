/**
 * Tipo de fragmento. Conteúdo de jogo — sem tabela `tipo_*` (§10.3). Fonte:
 * docs/core/sistema-v4.1.0.md — "Fragmentos" (os dois polos: Potencializador,
 * que melhora um item/ser, e Construtor, forma material estruturada).
 */
export enum FragmentoTipoEnum {
  POTENCIALIZADOR = 'POTENCIALIZADOR',
  CONSTRUTOR = 'CONSTRUTOR',
}
