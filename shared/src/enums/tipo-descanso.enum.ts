/**
 * Duração do descanso. Conteúdo de JSONB — sem tabela `tipo_*` (§10.3). Fonte:
 * docs/core/sistema-v4.1.0.md — "Descanso".
 */
export enum TipoDescansoEnum {
  CURTO = 'CURTO',
  MEDIO = 'MEDIO',
  LONGO = 'LONGO',
}
